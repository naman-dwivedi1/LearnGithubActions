on:
  push:
    branches:
      - main
      - dev

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo run time and hello world
        run: echo "hello world for push event"
